// Package causal hosts the gRPC client adapter that implements
// domain.CausalEngine on top of services/causal-inference. The full
// implementation lands in Phase 6 Stage 4 (Pathfinder shard); this doc.go is
// a placeholder so the .arch.yaml component declaration resolves cleanly
// before that shard runs.
package causal
