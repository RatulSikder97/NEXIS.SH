[pytest]
# Phase 6 — self-contained pytest config for the Hypothesis property
# suite. Kept separate from the parent fixtures/pytest.ini so the
# property tests can pick up whatever fixture tree the sidecar prepends
# to PYTHONPATH at run time, without ``pythonpath = src`` shadowing it.
testpaths = .
addopts = -q
