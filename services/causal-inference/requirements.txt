fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0

# Dev / test only — kept here so the same image can run pytest. Phase 7
# splits dev-only deps into a requirements-dev.txt when DoWhy ships.
pytest>=8.0
httpx>=0.27.0
